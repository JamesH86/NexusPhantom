import XCTest
@testable import NexusPhantomCore

final class NexusPhantomCoreTests: XCTestCase {
    func testExample() throws {
        // Basic test to ensure the module compiles
        XCTAssertTrue(true)
    }
}
